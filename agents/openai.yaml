interface:
  display_name: "XHS TikHub Feishu Ingest"
  short_description: "One-step setup and XHS-to-Feishu ingestion"
  default_prompt: "Use $xhs-tikhub-feishu-ingest to start initialization, then collect Xiaohongshu links into my local library and Feishu Base."
policy:
  allow_implicit_invocation: true
